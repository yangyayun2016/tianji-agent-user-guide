# CLAUDE.md

本文件为在本仓库中工作的 Claude Code 提供指引。

## 仓库定位

这是**天机智能体的用户使用指南**站点内容仓库。

天机智能体是一个面向跨境电商卖家的 AI 数据分析助手：把 SIF、卖家精灵、Sorftime、FlyFus、领星 ERP 等平台接进一个对话框，用大白话提问，助手自己取数、分析、出结论。

本仓库**只回答「怎么用」，不回答「怎么实现」**。读者是跨境电商的卖家、运营、广告优化师和选品经理——他们关心的是在哪个页面点什么、怎么提问才问得准、为什么没生效、扣了多少积分，不关心系统是怎么搭的。

## 内容红线（硬性约束）

写入 `docs/` 的任何内容，都不得包含：

- **架构与实现** —— 系统架构、模块划分、调用链路、技术选型
- **代码相关** —— 代码路径、类名/函数名、配置文件字段、代码片段
- **数据结构** —— 数据库表名、字段名、索引、数据模型
- **部署与运行环境** —— 部署方式、构建流程、容器、端口、进程、日志路径
- **服务器信息** —— 任何 IP 地址、主机名、内网地址、SSH 连接方式
- **任何凭据** —— API Key、Secret、数据库密码、默认账号与默认口令

涉及需要用户自己填写的密钥时，只写「填什么」和「去哪拿」（例如「登录平台 → 账户设置 → API 密钥」），**永远不写示例密钥值**，形态提示最多到 `ak_mcp_live_...` 这种前缀级别。

站点地址一律写成占位形式（`https://你的天机站点/...`），不写真实域名或 IP。

> 站点自身的构建与发布配置（若存在于仓库中）不属于文档正文的一部分，不要把这类内容写进 `docs/`。

## 目录结构

```
docs/       13 篇用户指南（本仓库的全部产出）
prompts/    需求流水，按时间顺序保存用户原始提示词，仅作记录
```

`docs/` 的四层结构，新增内容时按这个层次找位置：

| 层 | 文件 | 作用 |
|---|---|---|
| 认知 | `introduction.md`、`tianji-agent.md` | 产品是什么、适合谁、跟通用 AI 的区别、路线图 |
| 上手 | `getting-started.md` | 注册 → 选模型 → 配连接器 → 第一次提问 → 出报告 |
| 功能 | `chat.md`、`skills.md`、`connectors.md`、`knowledge-base.md`、`database.md`、`reports-and-schedule.md`、`feishu.md`、`credits.md` | 一个功能模块一页 |
| 兜底 | `admin.md`（仅站点管理员）、`faq.md`（横向汇总各模块高频问题） |

图片素材分两处存放，各自只有一个家：

- `docs/images/` —— **文档正文里引用的图**（截图、流程图）。在 md 里用相对路径引用（`./images/xxx.svg`），这样在站点和 GitHub 上都能正常显示。
- `docs/public/images/` —— **站点全局素材**（logo、favicon）。在站点配置和首页 hero 里用绝对路径引用（`/images/logo.svg`），构建时会自动补上 base 前缀。

文件名用小写短横线，语义化命名（如 `chat-tool-card.png`）。

## 写作规范

**语气**：跟同行讲话，不是写说明书。用「你」称呼读者，用大白话，能说人话就不用术语。

**结构**：每页开头一句话讲清这页解决什么问题；结尾给「使用建议 / 提问技巧」。功能页的标准骨架是 —— 这是什么 → 怎么配 → 怎么用 → 能问什么（带示例提问）→ 常见问题 → 建议。

**表格优先**：能力边界、字段速查、状态含义、模型单价这类信息一律用表格，比段落好扫。

**示例提问用代码块**：所有可以直接复制去用的提问，放在 ``` 代码块里，写成真实场景的样子。

**诚实标注**：未上线的功能明确写「功能预览，尚未正式启用」或「规划中」，不要写得像已经能用。

**安全提示要出现在会出事的地方**：分享链接是公开的、Webhook URL 不要外传、数据库建议用只读账号、管理员首次登录改密码 —— 这类提示写在对应功能页，不要只堆在 FAQ。

**交叉引用用相对链接**：`[连接器](./connectors.md)`，跨页概念不重复解释，指过去即可。

## 事实基线（改动时保持一致）

这些口径贯穿多个文件，改一处要全仓库同步：

- **激活方式**：连接器、知识库、数据库都靠在输入框里 `@` 选中才生效。「配了但不生效」的唯一答案是没 `@`。
- **积分**：每次对话按所选模型扣一次，与回答长短、工具调用次数无关；回答未完成不扣；余额不足直接拦下；定时任务同样扣。四档单价（1 / 2 / 2 / 5）在 `getting-started.md`、`credits.md`、`faq.md` 三处出现。
- **过程可见**：工具卡片可展开看原始数据、数据库卡片可展开看实际 SQL、知识库卡片可展开看命中片段。「结论存疑就展开卡片」是全站统一的排障话术。
- **连接器共 7 个**，其中 SIF 广告透视仪与 SIF 坑位监控共用同一个密钥，FlyFus MCP 与 FlyFus API 共用同一个 Key，且都不要自己加 `Bearer ` 前缀。
- **技能共 4 个**（选品 / 广告 / 数据 / 关键词分析师），会自动激活对应连接器，但不会替用户填密钥。
- **对话上限 20 轮**，是刻意限制而非缺陷。
- **数据库仅支持 MySQL**，默认只读（SELECT / SHOW / DESCRIBE / EXPLAIN），写操作必须用户点确认，单次 200 行 / 30 秒上限。
- **飞书是轻量问答入口**，不接连接器、知识库、数据库、技能，无多轮上下文。查数据一律回网页端。

## 改动前的检查清单

1. 有没有引入红线内容（架构 / 代码 / 表 / 部署 / IP / 凭据）？
2. 改动的说法在其他文件里是否也出现过？需不需要同步？
3. 相对链接和图片路径是否都能正常访问？
4. 新增的示例提问，用户能否直接复制去用？
5. 涉及未上线功能，有没有如实标注？

## Git 提交

完成修改后，在回复末尾给出 Conventional Commits 格式的 `git commit -m "..."` 命令供用户手动执行，**不要自动执行 git commit**。type 用 `docs`（文档内容）、`feat`（新增页面/素材）、`fix`（勘误）、`chore`（杂项）。
